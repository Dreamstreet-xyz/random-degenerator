import { Section, Container, Content, Header, Title } from '../sharedStyles';

export default function Leaderboard() {
    return (
        <Section>
            <Container>
                <Header>
                    <Title>Leaderboard</Title>
                </Header>
                <Content></Content>
            </Container>
        </Section>
    );
}
